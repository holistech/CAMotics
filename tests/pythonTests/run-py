#!/bin/bash
# Runs a Python test script against the built camotics.so module.
# Sets PYTHONPATH to the build directory. On a crash (refcount underflow)
# the test fails via the exit code.
set -u
here="$(cd "$(dirname "$0")" && pwd)"
export PYTHONPATH="$here/../../build"
exec python3 "$1"
